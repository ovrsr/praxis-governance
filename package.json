{
  "name": "praxis-governance",
  "version": "1.0.0",
  "description": "Governance infrastructure for the Praxis/EAL/OpenClaw agent network",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/shared && npm run build --workspaces",
    "test": "npm test --workspaces",
    "clean": "rm -rf packages/*/dist packages/*/node_modules"
  }
}
